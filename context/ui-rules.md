# Quiztin — UI Rules

> ⏳ **PENDING.** Generated **from** the Claude Design export once it exists (see `claude-design-handoff.md`).

When generated, this file will open with a **§0 prime directive** derived from the brand voice in `foundation.md`, then cover layout/density, hierarchy, color discipline (tokens only — see `ui-tokens.md`), per-surface rules (teacher authoring vs. student take-quiz vs. results), and the required interaction states (default/loading/disabled/error/empty).

Design intent already captured in the corpus (to fold in when this is generated): the take-quiz surface should be calm and low-anxiety with clear progress/state feedback and confirmation before irreversible actions (submit); quiz statuses are color-coded (available/scheduled/completed/closed); AI feedback is visually distinct and expandable; everything is keyboard-navigable and screen-reader friendly. (Source: the UI/UX briefs in `docs/` — `uc06-ui-ux-brief.md`, `uc08-ui-ux-brief.md`, `uc14-ui-ux-brief.md`.)
