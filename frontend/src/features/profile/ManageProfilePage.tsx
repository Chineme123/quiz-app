/**
 * Placeholder. The real role-aware profile form (UC14) is build-plan task 7.
 * It renders inside the authenticated app shell.
 */
export function ManageProfilePage() {
  return (
    <div className="mx-auto max-w-reading">
      <h1 className="font-display text-3xl text-text-strong">Manage your profile</h1>
      <p className="mt-3 text-text-body">The profile form arrives in the next build.</p>
    </div>
  );
}
