# Dev proxy targets (Vite reads these in vite.config.ts). Copy to `.env` to override.
# Defaults match the services' http launch profiles. Docker maps differ (5001/5002/5224).
AUTH_ORIGIN=http://localhost:5005
USER_ORIGIN=http://localhost:5079
QUIZ_ORIGIN=http://localhost:5224
