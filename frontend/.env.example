# Dev proxy target (Vite reads this in vite.config.ts). Copy to `.env` to override.
# All /api requests proxy to the YARP gateway, which routes to the right service.
# Default is the gateway's docker-compose host port.
API_ORIGIN=http://localhost:8080
